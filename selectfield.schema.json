{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "/src/Schemas/selectfield.schema.json",
  "title": "Select field",
  "description": "Property is editable with a select field",
  "additionalProperties": {
    "$ref": "./editproperty.schema.json"
  },
  "properties": {
    "options": {
      "type": "array",
      "description": "Selectable item. If this property is not set here, it has to be set programmatically (such as dynamically changing options)."
    },
    "optionLabelProp": {
      "type": "string",
      "description": "Property to search from this item for the label. If not set, property value itself will be used."
    }
  }
}
