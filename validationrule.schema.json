{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "/src/Schemas/validationrule.schema.json",
  "title": "Validation rule",
  "description": "Rule for validating a property.",
  "type": "object",
  "required": ["rule", "params"],
  "properties": {
    "rule": {
      "type": "string",
      "description": "Name of yup rule to apply (e.g. 'string' or 'required')."
    },
    "params": {
      "description": "Parameters for this yup rule. Will be passed in order to rule function. If using 'when' as a rule, first parameter is the name of the other property to look for and second parameter is an array of the rules to apply, if evaluates to true.",
      "type": "array",
      "items": {
        "anyOf": [
          { "type": "string" },
          { "type": "object" },
          { "type": "boolean" },
          { "type": "array" },
          { "type": "number" },
          { "type": "integer" },
          { "$ref": "./validationrule.schema.json" }
        ]
      }
    }
  }
}
