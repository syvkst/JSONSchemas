{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "/src/Schemas/column.schema.json",
  "title": "Table column",
  "description": "A column in a datatable",
  "type": "object",
  "required": ["id", "name"],
  "properties": {
    "id": {
      "description": "Database id for an entry.",
      "type": "string"
    },
    "name": {
      "description": "Display name for this column in a localized form.",
      "type": "array",
      "items": {
        "$ref": "./localized_string.schema.json"
      }
    },
    "enableSort": {
      "description": "If true, this column can be sorted. Default is false.",
      "type": "boolean"
    },
    "align": {
      "description": "The alignment of the column name inside its area. Default is left.",
      "type": "string",
      "enum": ["center", "inherit", "justify", "left", "right"]
    },
    "hidden": {
      "description": "If true, this column is a hidden column. Default is false.",
      "type": "boolean"
    },
    "labelProp": {
      "description": "If defined, label items based on a given property (rather than their name).",
      "type": "string"
    },
    "sortOnDataPropertyName": {
      "description": "If defined, sort items based on a given property (rather than their name).",
      "type": "string"
    }
  }
}
